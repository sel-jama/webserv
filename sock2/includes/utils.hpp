#ifndef UTILS_HPP
#define UTILS_HPP

#include <string>

int iskey(const std::string &);
int iskey2(const std::string &);
int containalpha(const std::string &);
int alldigit(const std::string &);

#endif